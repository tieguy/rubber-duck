"""Letta tool source files.

Each file in this directory contains a single tool function that gets
loaded and sent to Letta's sandbox for execution. Tools must be
self-contained (no imports from our codebase).
"""
